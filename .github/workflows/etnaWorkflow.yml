name: Etna CI CD
on: 
  pull_request: null
  push:
    branches:
    - feature/*

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
jobs:
  #Build:
    #runs-on: ubuntu-latest
    #container: dawidblom/etna:latest
    #steps:
    #- name: Checkout Code
    #  uses: actions/checkout@v3
    #- name: Build
    #  run: scripts/Build.sh   
      
  Test:
    runs-on: ubuntu-latest
    container: dawidblom/etna:latest       
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    #- name: Static Code Analysis
      #run: scripts/StaticCodeAnalysis.sh

    #- name: Unit Tests
      #run: |
      #  scripts/UnitTests.sh 
      #env:
      #  CPPUTEST_HOME: ${{ vars.CPPUTEST_HOME }}

    - name: Code Coverage
      run: |
        scripts/CodeCoverage.sh
      env: 
        CPPUTEST_HOME: ${{ vars.CPPUTEST_HOME }}
    - name: Publish Report
      run: |
        git clone https://user:$GITHUB_TOKEN@github.com/DJBlom/Coverage.git
        git config --global user.email "dawidjblom@gmail.com"
        git config --global user.name "Dawid"
        cp -R build/codeCoverage/html Coverage/
        cd Coverage
        git add .
        git commit -m "Coverage Report Updated"
        git config --unset-all http.https://github.com/.extraheader
        #git push --set-upstream https://user:$GITHUB_TOKEN@github.com/Coverage.git:main
        

      
        


     
  #HILTesting:
    #runs-on: ubuntu-latest
    #container: dawidblom/etna:latest
    #steps:
    #- name: Checkout Code
    #  uses: actions/checkout@v3
      
    #- name: Performing HIL Testing
    #  run:
    #    echo "Performing Hardware In The Loop Testing"
